# Django Settings
SECRET_KEY=your-secret-key-here
DEBUG=True
ALLOWED_HOSTS=127.0.0.1,localhost

# Database Configuration (Local Development)
DB_ENGINE=django.db.backends.postgresql
DB_NAME=Karura-sda
DB_USER=postgres
DB_PASSWORD=your-database-password
DB_HOST=127.0.0.1
DB_PORT=5432
DB_SSLMODE=prefer

# Production Database Configuration
# For shared hosting with Unix socket connection:
# - Leave DB_HOST empty or comment it out to use Unix socket
# - Remove DB_PORT or leave empty
# - Set DB_SSLMODE=disable if SSL is not supported
#
# DB_NAME=pkxjocmf_sdakarura
# DB_USER=pkxjocmf_karura
# DB_PASSWORD=your-production-password
# DB_HOST=
# DB_PORT=
# DB_SSLMODE=disable
